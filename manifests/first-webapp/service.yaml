apiVersion: v1
kind: Service

metadata:
  name: httpd-service
  namespace: first-webapp
  labels:
    app: first-webapp

spec:
  selector:
    app: first-webapp

  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80

  type: LoadBalancer