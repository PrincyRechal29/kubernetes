apiVersion: v1
kind: Service

metadata:
  name: py-animie-app
  namespace: py-animie-app
  labels:
    app: py-animie-app

spec:
  selector:
    app: py-animie-app

  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 5000

  type: LoadBalancer