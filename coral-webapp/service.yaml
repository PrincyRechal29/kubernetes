apiVersion: v1
kind: Service

metadata:
  name: coral-webapp-service
  namespace: coral-webapp
  labels:
    app: coral-webapp

spec:
  selector:
    app: coral-webapp

  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80

  type: LoadBalancer