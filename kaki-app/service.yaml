apiVersion: v1
kind: Service

metadata:
  name: kaki-webapp-service
  namespace: kaki-app
  labels:
    app: kaki-webapp

spec:
  selector:
    app: kaki-webapp

  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80

  type: LoadBalancer